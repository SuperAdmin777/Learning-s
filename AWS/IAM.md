IAM is not but just an Internal Assess Management we just create user , groups and policies in it.

Creating groups and user who are basically IAM users are easy, in production we always not work with root user so for that we create groups and users who are given admin power using policies to work in console.